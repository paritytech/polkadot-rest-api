{
  "annotations": { "list": [] },
  "description": "Benchmark comparison: polkadot-rest-api vs substrate-api-sidecar — offline results from wrk + resource_monitor",
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "panels": [
    {
      "type": "row",
      "title": "Overview",
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 0 },
      "collapsed": false
    },
    {
      "type": "stat",
      "title": "Median RPS — rest-api",
      "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
      "gridPos": { "h": 4, "w": 4, "x": 0, "y": 1 },
      "fieldConfig": { "defaults": { "unit": "reqps", "color": { "mode": "fixed", "fixedColor": "green" }, "thresholds": { "mode": "absolute", "steps": [{"color": "green", "value": null}] } } },
      "options": { "graphMode": "none", "textMode": "value", "reduceOptions": { "calcs": ["lastNotNull"] } },
      "targets": [{ "rawQueryText": "WITH r AS (SELECT rps, ROW_NUMBER() OVER (ORDER BY rps) as rn, COUNT(*) OVER () as cnt FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' AND service LIKE '%rest-api%') SELECT ROUND(AVG(rps),1) as median_rps FROM r WHERE rn IN (cnt/2, cnt/2+1)", "queryType": "table", "refId": "A" }]
    },
    {
      "type": "stat",
      "title": "Median RPS — sidecar",
      "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
      "gridPos": { "h": 4, "w": 4, "x": 4, "y": 1 },
      "fieldConfig": { "defaults": { "unit": "reqps", "color": { "mode": "fixed", "fixedColor": "blue" }, "thresholds": { "mode": "absolute", "steps": [{"color": "blue", "value": null}] } } },
      "options": { "graphMode": "none", "textMode": "value", "reduceOptions": { "calcs": ["lastNotNull"] } },
      "targets": [{ "rawQueryText": "WITH r AS (SELECT rps, ROW_NUMBER() OVER (ORDER BY rps) as rn, COUNT(*) OVER () as cnt FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' AND service='sidecar') SELECT ROUND(AVG(rps),1) as median_rps FROM r WHERE rn IN (cnt/2, cnt/2+1)", "queryType": "table", "refId": "A" }]
    },
    {
      "type": "stat",
      "title": "Median P99 — rest-api",
      "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
      "gridPos": { "h": 4, "w": 4, "x": 8, "y": 1 },
      "fieldConfig": { "defaults": { "unit": "ms", "color": { "mode": "fixed", "fixedColor": "green" }, "thresholds": { "mode": "absolute", "steps": [{"color": "green", "value": null}] } } },
      "options": { "graphMode": "none", "textMode": "value", "reduceOptions": { "calcs": ["lastNotNull"] } },
      "targets": [{ "rawQueryText": "WITH r AS (SELECT p99_ms, ROW_NUMBER() OVER (ORDER BY p99_ms) as rn, COUNT(*) OVER () as cnt FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' AND service LIKE '%rest-api%') SELECT ROUND(AVG(p99_ms),1) as median_p99 FROM r WHERE rn IN (cnt/2, cnt/2+1)", "queryType": "table", "refId": "A" }]
    },
    {
      "type": "stat",
      "title": "Median P99 — sidecar",
      "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
      "gridPos": { "h": 4, "w": 4, "x": 12, "y": 1 },
      "fieldConfig": { "defaults": { "unit": "ms", "color": { "mode": "fixed", "fixedColor": "blue" }, "thresholds": { "mode": "absolute", "steps": [{"color": "blue", "value": null}] } } },
      "options": { "graphMode": "none", "textMode": "value", "reduceOptions": { "calcs": ["lastNotNull"] } },
      "targets": [{ "rawQueryText": "WITH r AS (SELECT p99_ms, ROW_NUMBER() OVER (ORDER BY p99_ms) as rn, COUNT(*) OVER () as cnt FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' AND service='sidecar') SELECT ROUND(AVG(p99_ms),1) as median_p99 FROM r WHERE rn IN (cnt/2, cnt/2+1)", "queryType": "table", "refId": "A" }]
    },
    {
      "type": "stat",
      "title": "Peak RSS — rest-api",
      "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
      "gridPos": { "h": 4, "w": 4, "x": 16, "y": 1 },
      "fieldConfig": { "defaults": { "unit": "decmbytes", "color": { "mode": "fixed", "fixedColor": "green" }, "thresholds": { "mode": "absolute", "steps": [{"color": "green", "value": null}] } } },
      "options": { "graphMode": "none", "textMode": "value", "reduceOptions": { "calcs": ["lastNotNull"] } },
      "targets": [{ "rawQueryText": "SELECT ROUND(MAX(peak_rss_mb),1) FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' AND service LIKE '%rest-api%'", "queryType": "table", "refId": "A" }]
    },
    {
      "type": "stat",
      "title": "Peak RSS — sidecar",
      "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
      "gridPos": { "h": 4, "w": 4, "x": 20, "y": 1 },
      "fieldConfig": { "defaults": { "unit": "decmbytes", "color": { "mode": "fixed", "fixedColor": "blue" }, "thresholds": { "mode": "absolute", "steps": [{"color": "blue", "value": null}] } } },
      "options": { "graphMode": "none", "textMode": "value", "reduceOptions": { "calcs": ["lastNotNull"] } },
      "targets": [{ "rawQueryText": "SELECT ROUND(MAX(peak_rss_mb),1) FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' AND service='sidecar'", "queryType": "table", "refId": "A" }]
    },
    {
      "type": "row",
      "title": "Throughput",
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 5 },
      "collapsed": false
    },
    {
      "type": "barchart",
      "title": "RPS per Run",
      "description": "Each bar is one benchmark run. Shows consistency across runs.",
      "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 6 },
      "fieldConfig": {
        "defaults": { "unit": "reqps" },
        "overrides": [
          { "matcher": { "id": "byName", "options": "rest-api" }, "properties": [{ "id": "color", "value": { "fixedColor": "green", "mode": "fixed" } }] },
          { "matcher": { "id": "byName", "options": "sidecar" }, "properties": [{ "id": "color", "value": { "fixedColor": "blue", "mode": "fixed" } }] }
        ]
      },
      "options": { "orientation": "vertical", "showValue": "always", "barWidth": 0.6, "groupWidth": 0.7 },
      "targets": [
        { "rawQueryText": "SELECT 'Run ' || run_number as run, ROUND(MAX(CASE WHEN service LIKE '%rest-api%' THEN rps END), 1) as \"rest-api\", ROUND(MAX(CASE WHEN service = 'sidecar' THEN rps END), 1) as \"sidecar\" FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' GROUP BY run_number ORDER BY run_number", "queryType": "table", "refId": "A" }
      ]
    },
    {
      "type": "barchart",
      "title": "Avg Latency per Run (ms)",
      "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 6 },
      "fieldConfig": {
        "defaults": { "unit": "ms" },
        "overrides": [
          { "matcher": { "id": "byName", "options": "rest-api" }, "properties": [{ "id": "color", "value": { "fixedColor": "green", "mode": "fixed" } }] },
          { "matcher": { "id": "byName", "options": "sidecar" }, "properties": [{ "id": "color", "value": { "fixedColor": "blue", "mode": "fixed" } }] }
        ]
      },
      "options": { "orientation": "vertical", "showValue": "always", "barWidth": 0.6, "groupWidth": 0.7 },
      "targets": [
        { "rawQueryText": "SELECT 'Run ' || run_number as run, ROUND(MAX(CASE WHEN service LIKE '%rest-api%' THEN avg_latency_ms END), 1) as \"rest-api\", ROUND(MAX(CASE WHEN service = 'sidecar' THEN avg_latency_ms END), 1) as \"sidecar\" FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' GROUP BY run_number ORDER BY run_number", "queryType": "table", "refId": "A" }
      ]
    },
    {
      "type": "barchart",
      "title": "Error Rate per Run",
      "description": "Total errors per run. Zero is ideal.",
      "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 14 },
      "fieldConfig": {
        "defaults": {},
        "overrides": [
          { "matcher": { "id": "byName", "options": "rest-api" }, "properties": [{ "id": "color", "value": { "fixedColor": "green", "mode": "fixed" } }] },
          { "matcher": { "id": "byName", "options": "sidecar" }, "properties": [{ "id": "color", "value": { "fixedColor": "blue", "mode": "fixed" } }] }
        ]
      },
      "options": { "orientation": "vertical", "showValue": "always", "barWidth": 0.6, "groupWidth": 0.7 },
      "targets": [
        { "rawQueryText": "SELECT 'Run ' || run_number as run, MAX(CASE WHEN service LIKE '%rest-api%' THEN errors_total END) as \"rest-api\", MAX(CASE WHEN service = 'sidecar' THEN errors_total END) as \"sidecar\" FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' GROUP BY run_number ORDER BY run_number", "queryType": "table", "refId": "A" }
      ]
    },
    {
      "type": "table",
      "title": "Summary — Median Across Runs",
      "description": "Median of each metric computed independently across all runs",
      "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 14 },
      "fieldConfig": {
        "defaults": {},
        "overrides": [
          { "matcher": { "id": "byName", "options": "median_rps" }, "properties": [{ "id": "unit", "value": "reqps" }, { "id": "decimals", "value": 1 }] },
          { "matcher": { "id": "byName", "options": "median_latency" }, "properties": [{ "id": "unit", "value": "ms" }, { "id": "decimals", "value": 1 }] },
          { "matcher": { "id": "byName", "options": "median_p50" }, "properties": [{ "id": "unit", "value": "ms" }, { "id": "decimals", "value": 1 }] },
          { "matcher": { "id": "byName", "options": "median_p99" }, "properties": [{ "id": "unit", "value": "ms" }, { "id": "decimals", "value": 1 }] },
          { "matcher": { "id": "byName", "options": "peak_rss" }, "properties": [{ "id": "unit", "value": "decmbytes" }, { "id": "decimals", "value": 1 }] },
          { "matcher": { "id": "byName", "options": "avg_cpu" }, "properties": [{ "id": "unit", "value": "percent" }, { "id": "decimals", "value": 1 }] }
        ]
      },
      "targets": [{ "rawQueryText": "SELECT service, COUNT(*) as runs, ROUND(AVG(rps),1) as median_rps, ROUND(AVG(avg_latency_ms),1) as median_latency, ROUND(AVG(p50_ms),1) as median_p50, ROUND(AVG(p99_ms),1) as median_p99, ROUND(MAX(peak_rss_mb),1) as peak_rss, ROUND(AVG(avg_cpu_pct),1) as avg_cpu, SUM(total_requests) as total_reqs, SUM(errors_total) as total_errors FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' GROUP BY service ORDER BY service", "queryType": "table", "refId": "A" }]
    },
    {
      "type": "row",
      "title": "Latency Percentiles",
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 22 },
      "collapsed": false
    },
    {
      "type": "barchart",
      "title": "Latency Percentiles — rest-api vs sidecar",
      "description": "Average latency at each percentile across all runs. Green = rest-api, Blue = sidecar.",
      "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
      "gridPos": { "h": 10, "w": 24, "x": 0, "y": 23 },
      "fieldConfig": {
        "defaults": { "unit": "ms" },
        "overrides": [
          { "matcher": { "id": "byName", "options": "rest-api" }, "properties": [{ "id": "color", "value": { "fixedColor": "green", "mode": "fixed" } }] },
          { "matcher": { "id": "byName", "options": "sidecar" }, "properties": [{ "id": "color", "value": { "fixedColor": "blue", "mode": "fixed" } }] }
        ]
      },
      "options": { "orientation": "vertical", "showValue": "always", "barWidth": 0.5, "groupWidth": 0.7 },
      "targets": [
        { "rawQueryText": "SELECT 'p50' as percentile, ROUND(MAX(CASE WHEN service LIKE '%rest-api%' THEN avg_p END),1) as \"rest-api\", ROUND(MAX(CASE WHEN service='sidecar' THEN avg_p END),1) as \"sidecar\" FROM (SELECT service, AVG(p50_ms) as avg_p FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' GROUP BY service) UNION ALL SELECT 'p75', ROUND(MAX(CASE WHEN service LIKE '%rest-api%' THEN avg_p END),1), ROUND(MAX(CASE WHEN service='sidecar' THEN avg_p END),1) FROM (SELECT service, AVG(p75_ms) as avg_p FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' GROUP BY service) UNION ALL SELECT 'p90', ROUND(MAX(CASE WHEN service LIKE '%rest-api%' THEN avg_p END),1), ROUND(MAX(CASE WHEN service='sidecar' THEN avg_p END),1) FROM (SELECT service, AVG(p90_ms) as avg_p FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' GROUP BY service) UNION ALL SELECT 'p95', ROUND(MAX(CASE WHEN service LIKE '%rest-api%' THEN avg_p END),1), ROUND(MAX(CASE WHEN service='sidecar' THEN avg_p END),1) FROM (SELECT service, AVG(p95_ms) as avg_p FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' GROUP BY service) UNION ALL SELECT 'p99', ROUND(MAX(CASE WHEN service LIKE '%rest-api%' THEN avg_p END),1), ROUND(MAX(CASE WHEN service='sidecar' THEN avg_p END),1) FROM (SELECT service, AVG(p99_ms) as avg_p FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' GROUP BY service) UNION ALL SELECT 'p999', ROUND(MAX(CASE WHEN service LIKE '%rest-api%' THEN avg_p END),1), ROUND(MAX(CASE WHEN service='sidecar' THEN avg_p END),1) FROM (SELECT service, AVG(p999_ms) as avg_p FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' GROUP BY service)", "queryType": "table", "refId": "A" }
      ]
    },
    {
      "type": "barchart",
      "title": "P99 per Run — Side by Side",
      "description": "P99 latency from each run, both services overlaid",
      "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 31 },
      "fieldConfig": {
        "defaults": { "unit": "ms" },
        "overrides": [
          { "matcher": { "id": "byName", "options": "rest-api" }, "properties": [{ "id": "color", "value": { "fixedColor": "green", "mode": "fixed" } }] },
          { "matcher": { "id": "byName", "options": "sidecar" }, "properties": [{ "id": "color", "value": { "fixedColor": "blue", "mode": "fixed" } }] }
        ]
      },
      "options": { "orientation": "vertical", "showValue": "always", "barWidth": 0.5, "groupWidth": 0.7 },
      "targets": [
        { "rawQueryText": "SELECT 'Run ' || run_number as run, ROUND(MAX(CASE WHEN service LIKE '%rest-api%' THEN p99_ms END), 1) as \"rest-api\", ROUND(MAX(CASE WHEN service = 'sidecar' THEN p99_ms END), 1) as \"sidecar\" FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' GROUP BY run_number ORDER BY run_number", "queryType": "table", "refId": "A" }
      ]
    },
    {
      "type": "row",
      "title": "Process Resources",
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 39 },
      "collapsed": false
    },
    {
      "type": "trend",
      "title": "Memory (RSS) Over Time",
      "description": "RSS during baseline → load → cooldown. X axis = seconds elapsed. Green = rest-api, Blue = sidecar.",
      "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
      "gridPos": { "h": 10, "w": 12, "x": 0, "y": 40 },
      "fieldConfig": {
        "defaults": {
          "unit": "decmbytes",
          "custom": { "lineWidth": 2, "fillOpacity": 10, "showPoints": "never" }
        },
        "overrides": [
          { "matcher": { "id": "byName", "options": "rest-api" }, "properties": [{ "id": "color", "value": { "fixedColor": "green", "mode": "fixed" } }] },
          { "matcher": { "id": "byName", "options": "sidecar" }, "properties": [{ "id": "color", "value": { "fixedColor": "blue", "mode": "fixed" } }] }
        ]
      },
      "options": {
        "tooltip": { "mode": "multi" },
        "legend": { "calcs": ["min", "mean", "max"], "displayMode": "table", "placement": "bottom", "showLegend": true },
        "xField": "elapsed_s"
      },
      "targets": [
        { "rawQueryText": "SELECT a.elapsed_s, a.rss_mb as \"rest-api\", b.rss_mb as \"sidecar\" FROM resource_samples a LEFT JOIN resource_samples b ON a.elapsed_s = b.elapsed_s AND b.endpoint='${endpoint}' AND b.service='sidecar' WHERE a.endpoint='${endpoint}' AND a.service='rest-api' ORDER BY a.elapsed_s", "queryType": "table", "refId": "A" }
      ]
    },
    {
      "type": "trend",
      "title": "CPU Usage Over Time",
      "description": "CPU % during baseline → load → cooldown. 100% = 1 core. X axis = seconds elapsed.",
      "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
      "gridPos": { "h": 10, "w": 12, "x": 12, "y": 40 },
      "fieldConfig": {
        "defaults": {
          "unit": "percent",
          "custom": { "lineWidth": 2, "fillOpacity": 10, "showPoints": "never" }
        },
        "overrides": [
          { "matcher": { "id": "byName", "options": "rest-api" }, "properties": [{ "id": "color", "value": { "fixedColor": "green", "mode": "fixed" } }] },
          { "matcher": { "id": "byName", "options": "sidecar" }, "properties": [{ "id": "color", "value": { "fixedColor": "blue", "mode": "fixed" } }] }
        ]
      },
      "options": {
        "tooltip": { "mode": "multi" },
        "legend": { "calcs": ["mean", "max"], "displayMode": "table", "placement": "bottom", "showLegend": true },
        "xField": "elapsed_s"
      },
      "targets": [
        { "rawQueryText": "SELECT a.elapsed_s, a.cpu_pct as \"rest-api\", b.cpu_pct as \"sidecar\" FROM resource_samples a LEFT JOIN resource_samples b ON a.elapsed_s = b.elapsed_s AND b.endpoint='${endpoint}' AND b.service='sidecar' WHERE a.endpoint='${endpoint}' AND a.service='rest-api' ORDER BY a.elapsed_s", "queryType": "table", "refId": "A" }
      ]
    },
    {
      "type": "trend",
      "title": "Memory Growth Rate (RSS delta per sample)",
      "description": "Positive = memory growing, negative = shrinking. Sustained positive values under load may indicate a memory leak.",
      "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 50 },
      "fieldConfig": {
        "defaults": {
          "unit": "decmbytes",
          "custom": { "lineWidth": 1, "fillOpacity": 20, "showPoints": "never" }
        },
        "overrides": [
          { "matcher": { "id": "byName", "options": "rest-api" }, "properties": [{ "id": "color", "value": { "fixedColor": "green", "mode": "fixed" } }] },
          { "matcher": { "id": "byName", "options": "sidecar" }, "properties": [{ "id": "color", "value": { "fixedColor": "blue", "mode": "fixed" } }] }
        ]
      },
      "options": {
        "tooltip": { "mode": "multi" },
        "legend": { "calcs": ["mean", "max"], "displayMode": "table", "placement": "bottom", "showLegend": true },
        "xField": "elapsed_s"
      },
      "targets": [
        { "rawQueryText": "SELECT r.elapsed_s, ROUND((r.rss_mb - rp.rss_mb), 3) as \"rest-api\", ROUND((s.rss_mb - sp.rss_mb), 3) as \"sidecar\" FROM resource_samples r JOIN resource_samples rp ON r.elapsed_s = rp.elapsed_s + 1 AND rp.service='rest-api' AND rp.endpoint='${endpoint}' AND rp.session_id = r.session_id LEFT JOIN resource_samples s ON r.elapsed_s = s.elapsed_s AND s.service='sidecar' AND s.endpoint='${endpoint}' LEFT JOIN resource_samples sp ON s.elapsed_s = sp.elapsed_s + 1 AND sp.service='sidecar' AND sp.endpoint='${endpoint}' AND sp.session_id = s.session_id WHERE r.endpoint='${endpoint}' AND r.service='rest-api' ORDER BY r.elapsed_s", "queryType": "table", "refId": "A" }
      ]
    },
    {
      "type": "stat",
      "title": "Resource Summary",
      "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 50 },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" } },
        "overrides": []
      },
      "options": { "graphMode": "none", "textMode": "value_and_name", "orientation": "horizontal", "reduceOptions": { "calcs": ["lastNotNull"] } },
      "targets": [{ "rawQueryText": "SELECT service || ' Start RSS' as metric, ROUND(start_rss_mb, 1) as value FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' GROUP BY service UNION ALL SELECT service || ' Peak RSS', ROUND(MAX(peak_rss_mb), 1) FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' GROUP BY service UNION ALL SELECT service || ' End RSS', ROUND(end_rss_mb, 1) FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' GROUP BY service UNION ALL SELECT service || ' Avg CPU %', ROUND(avg_cpu_pct, 1) FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' GROUP BY service UNION ALL SELECT service || ' Peak CPU %', ROUND(peak_cpu_pct, 1) FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' GROUP BY service", "queryType": "table", "refId": "A" }]
    },
    {
      "type": "row",
      "title": "Correlation (Throughput vs Resources)",
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 58 },
      "collapsed": false
    },
    {
      "type": "trend",
      "title": "CPU vs Memory — rest-api",
      "description": "Dual axis: CPU (left) and RSS (right). Shows how memory grows with CPU load.",
      "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
      "gridPos": { "h": 10, "w": 12, "x": 0, "y": 59 },
      "fieldConfig": {
        "defaults": { "custom": { "lineWidth": 2, "fillOpacity": 10, "showPoints": "never" } },
        "overrides": [
          { "matcher": { "id": "byName", "options": "CPU %" }, "properties": [{ "id": "custom.axisPlacement", "value": "left" }, { "id": "unit", "value": "percent" }, { "id": "color", "value": { "fixedColor": "orange", "mode": "fixed" } }, { "id": "custom.fillOpacity", "value": 20 }] },
          { "matcher": { "id": "byName", "options": "RSS (MB)" }, "properties": [{ "id": "custom.axisPlacement", "value": "right" }, { "id": "unit", "value": "decmbytes" }, { "id": "color", "value": { "fixedColor": "green", "mode": "fixed" } }, { "id": "custom.lineWidth", "value": 2 }] }
        ]
      },
      "options": { "tooltip": { "mode": "multi" }, "legend": { "calcs": ["mean", "max"], "displayMode": "table", "placement": "bottom", "showLegend": true }, "xField": "elapsed_s" },
      "targets": [
        { "rawQueryText": "SELECT elapsed_s, cpu_pct as \"CPU %\", rss_mb as \"RSS (MB)\" FROM resource_samples WHERE endpoint='${endpoint}' AND service='rest-api' ORDER BY elapsed_s", "queryType": "table", "refId": "A" }
      ]
    },
    {
      "type": "trend",
      "title": "CPU vs Memory — sidecar",
      "description": "Dual axis: CPU (left) and RSS (right).",
      "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
      "gridPos": { "h": 10, "w": 12, "x": 12, "y": 59 },
      "fieldConfig": {
        "defaults": { "custom": { "lineWidth": 2, "fillOpacity": 10, "showPoints": "never" } },
        "overrides": [
          { "matcher": { "id": "byName", "options": "CPU %" }, "properties": [{ "id": "custom.axisPlacement", "value": "left" }, { "id": "unit", "value": "percent" }, { "id": "color", "value": { "fixedColor": "orange", "mode": "fixed" } }, { "id": "custom.fillOpacity", "value": 20 }] },
          { "matcher": { "id": "byName", "options": "RSS (MB)" }, "properties": [{ "id": "custom.axisPlacement", "value": "right" }, { "id": "unit", "value": "decmbytes" }, { "id": "color", "value": { "fixedColor": "blue", "mode": "fixed" } }, { "id": "custom.lineWidth", "value": 2 }] }
        ]
      },
      "options": { "tooltip": { "mode": "multi" }, "legend": { "calcs": ["mean", "max"], "displayMode": "table", "placement": "bottom", "showLegend": true }, "xField": "elapsed_s" },
      "targets": [
        { "rawQueryText": "SELECT elapsed_s, cpu_pct as \"CPU %\", rss_mb as \"RSS (MB)\" FROM resource_samples WHERE endpoint='${endpoint}' AND service='sidecar' ORDER BY elapsed_s", "queryType": "table", "refId": "A" }
      ]
    },
    {
      "type": "row",
      "title": "Per-Run Details",
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 69 },
      "collapsed": false
    },
    {
      "type": "table",
      "title": "All Runs — Raw Data",
      "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
      "gridPos": { "h": 10, "w": 24, "x": 0, "y": 70 },
      "fieldConfig": {
        "defaults": {},
        "overrides": [
          { "matcher": { "id": "byName", "options": "rps" }, "properties": [{ "id": "unit", "value": "reqps" }, { "id": "decimals", "value": 1 }] },
          { "matcher": { "id": "byName", "options": "avg_latency_ms" }, "properties": [{ "id": "unit", "value": "ms" }, { "id": "decimals", "value": 1 }] },
          { "matcher": { "id": "byName", "options": "p50_ms" }, "properties": [{ "id": "unit", "value": "ms" }, { "id": "decimals", "value": 1 }] },
          { "matcher": { "id": "byName", "options": "p99_ms" }, "properties": [{ "id": "unit", "value": "ms" }, { "id": "decimals", "value": 1 }] },
          { "matcher": { "id": "byName", "options": "p999_ms" }, "properties": [{ "id": "unit", "value": "ms" }, { "id": "decimals", "value": 1 }] },
          { "matcher": { "id": "byName", "options": "peak_rss_mb" }, "properties": [{ "id": "unit", "value": "decmbytes" }, { "id": "decimals", "value": 1 }] },
          { "matcher": { "id": "byName", "options": "transfer_per_sec" }, "properties": [{ "id": "unit", "value": "Bps" }, { "id": "decimals", "value": 0 }] }
        ]
      },
      "targets": [{ "rawQueryText": "SELECT service, run_number as run, rps, avg_latency_ms, p50_ms, p99_ms, p999_ms, total_requests, errors_total as errors, peak_rss_mb, avg_cpu_pct, peak_cpu_pct, ROUND(transfer_per_sec, 0) as transfer_per_sec FROM benchmark_runs WHERE endpoint='${endpoint}' AND scenario='${scenario}' ORDER BY service, run_number", "queryType": "table", "refId": "A" }]
    }
  ],
  "refresh": "",
  "schemaVersion": 39,
  "tags": ["benchmark", "comparison", "polkadot", "rest-api", "sidecar"],
  "templating": {
    "list": [
      {
        "current": {},
        "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
        "definition": "SELECT DISTINCT endpoint FROM benchmark_runs ORDER BY endpoint",
        "name": "endpoint",
        "label": "Endpoint",
        "type": "query",
        "query": "SELECT DISTINCT endpoint FROM benchmark_runs ORDER BY endpoint",
        "refresh": 1,
        "sort": 1,
        "includeAll": false,
        "multi": false
      },
      {
        "current": {},
        "datasource": { "type": "frser-sqlite-datasource", "uid": "benchmarks" },
        "definition": "SELECT DISTINCT scenario FROM benchmark_runs ORDER BY scenario",
        "name": "scenario",
        "label": "Scenario",
        "type": "query",
        "query": "SELECT DISTINCT scenario FROM benchmark_runs ORDER BY scenario",
        "refresh": 1,
        "sort": 1,
        "includeAll": false,
        "multi": false
      }
    ]
  },
  "time": { "from": "2023-11-14T22:13:20.000Z", "to": "2023-11-14T22:25:00.000Z" },
  "title": "Benchmark Comparison: REST API vs Sidecar",
  "uid": "bench-compare"
}
